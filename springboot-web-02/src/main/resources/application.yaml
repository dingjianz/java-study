spring:
  application:
    name: springboot-web-02

  datasource:
    url: jdbc:mysql://localhost:3306/web02
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

  servlet:
    multipart:
      max-file-size: 10MB       # 单个文件最大大小
      max-request-size: 100MB    # 单次请求最大大小（包括所有文件和表单数据）

mybatis:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true
  mapper-locations: classpath:mapper/*.xml

logging:
  level:
    root: info                      # 根级别，未单独指定的包都用这个
    com.itheima: debug              # 自己的代码开到 debug，log.debug 才会输出
    org.springframework.jdbc.support.JdbcTransactionManager: debug
  pattern:
    console: "%d{HH:mm:ss.SSS} %highlight(%-5level) %cyan(%logger{36}) - %msg%n"


# AWS S3 配置（使用 digit-cloud-gc 项目的凭证）
aws:
  s3:
    url: oss-beijing-m8.openstorage.cn
    access-key: GL08C7Y3RZWAEWFDYT3V
    secret-key: 2fksKTc3NZeWEE1N6Nd3V1MW5uJVRGZVQVab55Ri
    bucket: cloud-gc
    max-connections: 200
    region: cn-north-1
