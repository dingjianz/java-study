<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itheima.mapper.EmpExprMapper">
    <!-- 根据员工 ID 查询工作经历列表 -->
    <select id="getByEmpId" resultType="com.itheima.pojo.EmpExpr">
        select * from emp_expr where emp_id = #{empId} order by begin_date desc
    </select>

    <!-- 批量插入员工工作经历 -->
    <insert id="insertBatch">
        insert into emp_expr(emp_id, begin_date, end_date, company, job)
        values
        <foreach collection="list" item="expr" separator=",">
            (#{expr.empId}, #{expr.beginDate}, #{expr.endDate}, #{expr.company}, #{expr.job})
        </foreach>
    </insert>
</mapper>
